```mediawiki
{| class="wikitable"
|-
! Ticket Key !! Summary !! Components !! Fix Versions !! Priority !! Issue Type !! Status !! Resolution !! Labels !! Description
|-
| SPL-280187 || [CLONE] [sustain/splunk-10.0] MachineTypeFilter doesn't work when DS works in clustered mode - applications are getting wrongfully updated into clients when performing OS filtering in serverclass || None || None || P2-High || Bug || Resolved || Fixed || None || This ticket is a backport of [https://splunk.atlassian.net/browse/SPL-270345|this issue].
|-
| SPL-280186 || [CLONE] [sustain/europium] MachineTypeFilter doesn't work when DS works in clustered mode - applications are getting wrongfully updated into clients when performing OS filtering in serverclass || None || None || P2-High || Bug || Resolved || Fixed || None || This ticket is a backport of [https://splunk.atlassian.net/browse/SPL-270345|this issue].
|-
| SPL-280185 || [CLONE] [sustain/duranium] MachineTypeFilter doesn't work when DS works in clustered mode - applications are getting wrongfully updated into clients when performing OS filtering in serverclass || None || None || P2-High || Bug || Resolved || Fixed || None || This ticket is a backport of [https://splunk.atlassian.net/browse/SPL-270345|this issue].
|-
| SPL-280184 || [CLONE] [sustain/cobalt] MachineTypeFilter doesn't work when DS works in clustered mode - applications are getting wrongfully updated into clients when performing OS filtering in serverclass || None || None || P2-High || Bug || Resolved || Fixed || None || This ticket is a backport of [https://splunk.atlassian.net/browse/SPL-270345|this issue].
|-
| SPL-279750 || After upgrade to 9.3.0+, UFs are reporting 'Failed to download app', while DS reports 'app does not exist on the server’. This issue does not appear if you configure the environment in such a way that each app is assigned to a maximum of one server class || None || None || P3-Medium || Bug || Closed || Fixed || None || Deployment of apps using Deployment Server not functioning properly after upgrade from 9.2.2 to 9.3.1.
|-
| SPL-279657 || Distributed search may lose information about bundles from cluster peers on new search generations, removes peers when best-effort mode is enabled. || None || Pocky || P1-Immediate || Bug || Closed || Fixed || None || Information exposure issue with search generation changes causing disruptions.
|-
| SPL-278716 || KV store upgrade to server version 7.0 fails when SSL compression is not set to its default value. || None || None || P2-High || Bug || Closed || Fixed || None || Customer issues encountered during KV store upgrade due to SSL compression settings.
|-
| SPL-278604 || Release notes content unavailable in docs for some versions of universal forwarder || None || None || Unknown || Bug || Untriaged ||  || None || Need manual recreation of UF known and fixed issues information following documentation migration.
|-
| SPL-277301 || The default sourcetype has 'NO_BINARY_CHECK' as 'true' on Web UI while it's not set in props.conf || None || None || P3-Medium || Bug || Closed || Fixed || None || Discrepancy found between Web UI settings for default sourcetypes and actual props.conf.
|-
| SPL-273098 || [PUBLIC] "Show Source" UI returns a javascript error when it should return a 404 error || None || None || P2-High || Bug || Resolved || Fixed || None || Issue with "Show Source" functionality returning incorrect error.
|}
```